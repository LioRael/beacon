interface:
  display_name: "Beacon"
  short_description: "Check and safely update development toolchains"
  default_prompt: "Use $beacon to check my development environment and prepare safe updates."
policy:
  allow_implicit_invocation: true
